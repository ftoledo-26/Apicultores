USE empresa;

-- Tabla de usuarios
CREATE TABLE usuarios (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre NVARCHAR(50) NOT NULL,
    email NVARCHAR(255) NOT NULL UNIQUE,
    contrasenia VARCHAR(255) NOT NULL
);

-- Tabla de categorías
CREATE TABLE categorias (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre NVARCHAR(100) NOT NULL UNIQUE
);

-- Tabla de libros
CREATE TABLE libros (
    id INT AUTO_INCREMENT PRIMARY KEY,
    titulo NVARCHAR(255) NOT NULL,
    autor NVARCHAR(255),
    id_categoria INT,
    FOREIGN KEY (id_categoria) REFERENCES categorias(id)
);

-- Tabla de comentarios
CREATE TABLE comentarios (
    id INT AUTO_INCREMENT PRIMARY KEY,
    id_usuario INT NOT NULL,
    id_libro INT NOT NULL,
    comentario TEXT NOT NULL,
    fecha DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (id_usuario) REFERENCES usuarios(id),
    FOREIGN KEY (id_libro) REFERENCES libros(id)
);

INSERT INTO usuarios (nombre, email, contrasenia) VALUES
('Juan Pérez', 'juan@example.com', 'hash1'),
('María López', 'maria@example.com', 'hash2'),
('Carlos García', 'carlos@example.com', 'hash3');


INSERT INTO categorias (nombre) VALUES
('Ficción'),
('Historia'),
('Tecnología'),
('Fantasía');


INSERT INTO libros (titulo, autor, id_categoria) VALUES
('Cien Años de Soledad', 'Gabriel García Márquez', 1),
('Sapiens: De animales a dioses', 'Yuval Noah Harari', 2),
('Clean Code', 'Robert C. Martin', 3),
('El Señor de los Anillos', 'J.R.R. Tolkien', 4);


INSERT INTO comentarios (id_usuario, id_libro, comentario) VALUES
(1, 1, 'Un libro increíble, lo recomiendo totalmente.'),
(2, 3, 'Muy útil para mejorar la calidad del código.'),
(3, 4, 'Una historia épica, me encantó.'),
(1, 2, 'Muy interesante, aprendí bastante.');
